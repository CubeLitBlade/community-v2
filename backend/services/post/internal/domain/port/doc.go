// Package port defines the secondary ports (driven interfaces) for the domain layer.
// It specifies the contracts for infrastructure dependencies—such as repositories,
// transaction management, authorization, and ID generation—allowing the core domain
// to remain completely decoupled from their concrete implementations.
package port
